cmake_minimum_required(VERSION 3.28)

add_subdirectory(engine)
if(COMMAND_LINE_ONLY)
    message(STATUS "Not compiling 'interface'")
else()
    message(STATUS "Compiling 'interface'")
    add_subdirectory(interface)
endif()
add_subdirectory(cl)
